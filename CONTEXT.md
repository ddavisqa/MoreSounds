The Core Objective: (e.g., "This script scrapes audio files and metadata from [Target Website].")

Key Mechanisms: (e.g., "It bypasses pagination by simulating API requests rather than rendering HTML.")

The Modernization Goal: (e.g., "We are upgrading to Python 3.12, moving to async requests, and packaging it with uv, but the core scraping logic and output format must remain identical.")

